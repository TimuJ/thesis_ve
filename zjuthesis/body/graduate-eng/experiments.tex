% Chapter 5: Experimental Validation
\chapter{Experimental Validation}
\label{ch:experiments}

% =========================================================================
% DRAFT SKELETON (Jul 11). All numbers below must be verified against the
% canonical sources before submission:
%   reports/figures/verdict_matrix_v5.md      (synthetic verdict matrix)
%   reports/figures/realmodel_v5_3method.md   (3-method ranking)
%   reports/figures/realmodel_v5_ranking.md   (sub-metric breakdown)
% =========================================================================

\section{Experimental Setup}
\label{sec:exp:setup}

\paragraph{Base videos.} Five long-form base videos (identified as 7WHI, BrRLK, KZ,
hhsz, and mJog throughout), $80$--$208$ seconds each, $2{,}412$--$5{,}000$ frames,
$22{,}412$ frames in total, degraded by the DOVE pipeline
(Section~\ref{sec:found:dove}) to $320\times180$ and super-resolved to
$1280\times720$.

\paragraph{Synthetic benchmark.} Twelve artefact families at five severities over the
five base videos ($300$ clips): the four families of
Section~\ref{sec:problem:battery} (colour drift, chunk-boundary, flicker, identity
degradation), two progressive long-range families (identity drift, background drift),
and a six-transform flip control family (horizontal, transpose, periodic, elastic,
channel-shuffle, invert) designed for sub-metric attribution with predicted-blind and
predicted-caught cells.

\paragraph{Evaluated methods.} MGLD-VSR \cite{he_mgld_2024}, Upscale-A-Video
\cite{zhou_upscale_2024}, FlashVSR \cite{zhuang_flashvsr_2025}
(Section~\ref{sec:found:vsr}), and --- as a designed contrast --- a
frame-wise anchor: RealESRGAN \cite{chan_realbasicvsr_2022} applied
independently per frame, i.e.\ super-resolution with \emph{no temporal
modelling at all}, which any temporal-consistency metric should be able to
tell apart from genuine video methods.

\section{Synthetic Artefact Validation}
\label{sec:exp:synthetic}

For each (artefact family, base video) condition, the composite's
severity-response is summarised by the score change from the lowest to the
highest severity, $\Delta = \text{LR-VCC}(\text{sev }0.40) -
\text{LR-VCC}(\text{sev }0.02)$, classified as \textsc{pass} ($\Delta \leq
-0.05$), \textsc{weak} ($\Delta \leq -0.02$), \textsc{flat} ($|\Delta| <
0.02$), or \textsc{inv}erted ($\Delta \geq +0.02$). Table~\ref{tab:exp:verdict}
presents the full matrix, recomposed uniformly under the canonical
configuration.

\begin{table}[h]
\centering
\caption{LR-VCC v5 severity-response verdict matrix: twelve artefact families
$\times$ five base videos. Each cell shows the verdict and $\Delta$ (severity
0.02 $\to$ 0.40). Clean (\textsc{pass}+\textsc{weak}): 29/60 conditions.
Source: \texttt{verdict\_matrix\_v5\_uniform.md}.}
\label{tab:exp:verdict}
\small
\setlength{\tabcolsep}{3.5pt}
\begin{tabular}{lccccc}
\hline
\textbf{Artefact} & \textbf{7WHI} & \textbf{BrRLK} & \textbf{KZ} & \textbf{hhsz} & \textbf{mJog} \\
\hline
colour\_drift & PASS ($-.11$) & PASS ($-.06$) & PASS ($-.15$) & WEAK ($-.05$) & PASS ($-.11$) \\
chunk\_boundary & PASS ($-.07$) & INV ($+.11$) & PASS ($-.10$) & PASS ($-.11$) & WEAK ($-.04$) \\
background\_drift & FLAT ($+.00$) & INV ($+.05$) & PASS ($-.07$) & PASS ($-.20$) & FLAT ($+.00$) \\
flicker & FLAT ($-.00$) & INV ($+.06$) & WEAK ($-.03$) & FLAT ($+.01$) & FLAT ($+.00$) \\
identity\_degradation & INV ($+.03$) & FLAT ($+.00$) & WEAK ($-.03$) & PASS ($-.05$) & FLAT ($+.01$) \\
identity\_drift & FLAT ($-.00$) & FLAT ($-.00$) & WEAK ($-.03$) & PASS ($-.07$) & FLAT ($-.00$) \\
\hline
flip\_invert & PASS ($-.15$) & PASS ($-.22$) & PASS ($-.35$) & PASS ($-.39$) & PASS ($-.07$) \\
flip\_channel\_shuffle & WEAK ($-.03$) & PASS ($-.08$) & PASS ($-.31$) & FLAT ($+.00$) & PASS ($-.06$) \\
flip\_transpose & FLAT ($+.01$) & FLAT ($+.00$) & PASS ($-.05$) & PASS ($-.06$) & WEAK ($-.03$) \\
flip\_periodic & FLAT ($-.01$) & FLAT ($-.02$) & FLAT ($+.00$) & FLAT ($-.02$) & FLAT ($-.01$) \\
flip\_elastic & FLAT ($-.00$) & FLAT ($+.00$) & FLAT ($-.00$) & FLAT ($+.01$) & WEAK ($-.02$) \\
flip\_horizontal & FLAT ($+.02$) & FLAT ($+.01$) & FLAT ($-.00$) & FLAT ($-.01$) & FLAT ($-.02$) \\
\hline
\end{tabular}
\end{table}

Four observations structure the reading of Table~\ref{tab:exp:verdict}.

\textbf{The colour-drift blind spot is closed.} Colour drift --- invisible to
every one of the eight baseline metrics in
Section~\ref{sec:problem:battery} --- is clean on all five bases (four
\textsc{pass}, one \textsc{weak}), the direct payoff of sub-metrics E and
D$'$.

\textbf{The v5 anchored redesign fixed the convergence pathology.} Under v4,
progressive background replacement \emph{inverted} on four of five bases ---
the composite rewarded the corruption. Under v5 a single inversion remains
(BrRLK, with halved magnitude), with the two strongest responses
(\textsc{pass} at $-0.20$ and $-0.07$) on the bases where the anchor is most
stable. The two \textsc{flat} cells share a mechanism: on content with
substantial natural scene change, the anchor distance saturates early and the
injected drift adds little.

\textbf{The flip control battery behaved as designed.} The flip family was
constructed with \emph{predicted} outcomes: flip\_invert (histogram-destroying
positive control) should be caught everywhere --- it is, \textsc{pass} on all
five bases; flip\_horizontal (histogram-preserving, CLIP-robust) should be
invisible --- it is, \textsc{flat} everywhere, a designed null result that
confirms the sub-metrics measure what they claim rather than any change
whatsoever; flip\_channel\_shuffle (marginal-preserving but
appearance-breaking) should be caught partially --- four of five clean.
Periodic and elastic transforms, predicted mostly invisible, are mostly
\textsc{flat}. The battery turns the verdict matrix from a scorecard into a
falsifiable instrument check.

\textbf{The residual failures are localised and mechanistic.} The BrRLK column
carries three of the five inversions: cartoon content with frequent hard scene
cuts, where natural anchor-distance variation dominates any injected signal
--- a documented content-domain limitation
(Section~\ref{sec:exp:limitations}). The identity rows are the weakest
families (the slow-fast pooling pathology of
Section~\ref{sec:problem:battery}, Finding~4, plus the parked dispersion
gate); flicker remains largely composite-invisible because the sub-metrics
that respond to it (T at $k{=}1$) rarely dominate the softmax --- both are
characterised, named failure modes rather than unexplained noise.

\section{Real-Model Evaluation}
\label{sec:exp:real}

Applying LR-VCC v5 to the three methods on the five-video real-SR test set yields the
ranking in Table~\ref{tab:exp:threemethod}. The canonical protocol applies the metric
as designed: fps-corrected identity inputs, the close-up reliability gate active for
every method, and the dispersion gate off (parked pending calibration data).

\begin{table}[h]
\centering
\caption{LR-VCC v5 composite per video and method under the canonical (uniformly
gated) protocol. Bold marks the per-video winner. RealESRGAN is the frame-wise
(no temporal modelling) anchor. Source: \texttt{realmodel\_v5\_gated.md}.}
\label{tab:exp:threemethod}
\begin{tabular}{lcccc}
\hline
\textbf{Video} & \textbf{MGLD-VSR} & \textbf{UAV} & \textbf{FlashVSR} & \textbf{RealESRGAN} \\
\hline
7WHI & \textbf{0.738} & 0.700 & 0.737 & 0.736 \\
BrRLK & \textbf{0.402} & 0.379 & 0.393 & 0.401 \\
KZ & \textbf{0.750} & 0.705 & 0.722 & 0.724 \\
hhsz & \textbf{0.566} & 0.545 & 0.550 & 0.529 \\
mJog & \textbf{0.654} & 0.617 & 0.649 & 0.631 \\
\hline
\textbf{Mean} & \textbf{0.622} & 0.589 & 0.610 & 0.604 \\
\hline
\end{tabular}
\end{table}

MGLD-VSR leads on every video and on the mean, though the margins against
FlashVSR are small (7WHI and mJog are near-ties at $0.001$ and $0.005$); UAV
is third throughout. The comparison is protocol-honest: an interim table
composed \emph{without} the close-up gate on two of the three methods ranked
FlashVSR first ($0.610$ vs $0.589$) --- the FlashVSR-vs-MGLD comparison
depends on how much weight the identity sub-metric receives, which is itself
quantified in the ablations below. The MGLD $>$ UAV ordering, by contrast, is
unconditional: it holds on every video under the canonical protocol and in 50
of 52 sensitivity configurations (Section~\ref{sec:exp:ablations}).

Table~\ref{tab:exp:submetrics} decomposes the composite. FlashVSR's
closeness to the top is driven by identity preservation ($0.598$ vs MGLD's
$0.557$ and UAV's $0.459$) --- and FlashVSR is simultaneously \emph{worst} on
D$''$, the sub-metric most sensitive to long-range semantic drift from the
anchor ($0.862$ vs $0.893$/$0.884$). A streaming model whose temporal
positions grow without bound, carrying one mid-video seam on four of five
videos, is exactly the design one would suspect of long-range drift; whether
position encoding is actually the cause is the question
Chapter~\ref{ch:rope} answers. UAV's single win is the temporal cell
($0.942$) --- the smoother-output bias of warping-error measures, visible
even inside the composite, and the reason T's reliability rarely dominates
the softmax.

\begin{table}[h]
\centering
\caption{Per-sub-metric means over the five videos (canonical protocol).
Bold marks the best method per sub-metric; underline marks the worst. Source:
\texttt{realmodel\_v5\_gated.md}.}
\label{tab:exp:submetrics}
\begin{tabular}{lcccc}
\hline
\textbf{Sub-metric} & \textbf{MGLD-VSR} & \textbf{UAV} & \textbf{FlashVSR} & \textbf{RealESRGAN} \\
\hline
A (CLIP-IQA appearance) & \textbf{0.447} & \underline{0.336} & 0.400 & 0.432 \\
T (tOF temporal) & 0.934 & \textbf{0.942} & 0.939 & \underline{0.930} \\
I (slow-fast identity) & 0.557 & 0.459 & \textbf{0.598} & \underline{0.448} \\
D (histogram stability) & 0.505 & 0.506 & \underline{0.501} & \textbf{0.508} \\
E (colour slope) & 0.339 & \textbf{0.341} & 0.336 & \underline{0.328} \\
D$'$ (anchor histogram) & \textbf{0.708} & \underline{0.687} & 0.705 & 0.699 \\
D$''$ (CLIP trajectory) & \textbf{0.893} & 0.884 & \underline{0.862} & 0.867 \\
\hline
\end{tabular}
\end{table}

\paragraph{The frame-wise anchor lands where it should.} RealESRGAN's
signature is the mirror image of a genuine video method: second-best
\emph{per-frame} appearance (its outputs are sharp) but the \emph{worst}
identity, temporal, and colour-slope cells of the four methods --- per-frame
processing flickers exactly where temporal modelling stabilises. Its composite
therefore sits mid-pack: above UAV, whose per-frame quality deficit outweighs
its smoothness advantage, and below both genuine temporal methods'
best-performing configurations. The anchor also sharpens an honest boundary of
the study: the composite separates the anchor from MGLD-VSR clearly on the
temporal-family cells, but multi-scale tOF alone barely does ($0.930$ vs
$0.934$) --- reinforcing that anchored drift and identity, not warping error,
carry the discriminative temporal signal on well-behaved methods.

\paragraph{Relation to per-frame quality metrics.} Per-frame no-reference
quality measures agree with LR-VCC's MGLD-vs-UAV ordering --- CLIP-IQA
($0.496$ vs $0.391$), MUSIQ ($65.1$ vs $56.3$), NIQE, BRISQUE, and DOVER
($73.8$ vs $65.1$) all favour the detail-preserving method --- but they carry
no consistency signal at all: Chapter~\ref{ch:problem}'s battery shows them
blind to every long-range artefact family, and they would score a
colour-drifting or identity-collapsing video identically to a stable one. The
division of labour is therefore sharp: per-frame metrics measure quality
without consistency; the SOTA consistency dimensions measure a
smoothness-confounded consistency that inverts against perception
(Table~\ref{tab:exp:vbench}); LR-VCC is the instrument that supplies a
consistency signal which is simultaneously perception-aligned.

% TODO(when server rows land): add RealESRGAN frame-wise anchor row and (if
% standup succeeds) SeedVR2 row to both tables; expected: anchor scores well
% on per-frame quality but poorly on temporal/drift cells — the
% "no-temporal-modelling" contrast. SeedVR2 gives the window-local-position
% D'' contrast against FlashVSR's absolute streaming positions. The SOTA
% severity verdicts (analyze_vbench_consistency output) extend §5.4.

\section{Comparison with the Closest Existing Benchmark}
\label{sec:exp:vbench}

The nearest published instruments to LR-VCC's goal --- the two
state-of-the-art baselines for video consistency assessment --- are VBench's
\cite{huang_vbench_2024} subject-consistency dimension (DINO-feature cosine
similarity between adjacent frames and against the first frame) and
background-consistency dimension (the same construction on CLIP features),
aggregated per video via the long-video clip protocol. A protocol note for
fairness: long-video SR input is not an officially supported configuration
for these dimensions (Section~\ref{sec:found:toolkit}) --- they are run here
through the \texttt{vbench2\_beta\_long} extension in custom-input mode,
i.e.\ the published method with documented adjustments, at its default
long-video weighting ($w_{\mathrm{inclip}}{=}1$,
$w_{\mathrm{clip2clip}}{=}0$). Table~\ref{tab:exp:vbench} evaluates both on
the real-SR test set alongside the degraded low-quality input itself.

\begin{table}[h]
\centering
\caption{VBench consistency dimensions on the real-SR test set (five-video
means), including the degraded LQ input as a row. Higher is better according
to the benchmark.}
\label{tab:exp:vbench}
\begin{tabular}{lccc}
\hline
\textbf{Dimension} & \textbf{LQ input} & \textbf{MGLD-VSR} & \textbf{UAV} \\
\hline
subject\_consistency & 0.8936 & 0.8927 & \textbf{0.9031} \\
background\_consistency & \textbf{0.9333} & 0.9235 & 0.9317 \\
\hline
\end{tabular}
\end{table}

Two readings disqualify these dimensions as long-video SR consistency
measures, and both follow from the smoother-output bias characterised in
Chapter~\ref{ch:problem}. First, \textbf{background consistency ranks the
degraded input above both of its super-resolutions} (and subject consistency
places it above MGLD): a blurry, detail-poor video produces more stable DINO
features than any faithful restoration of it, so the benchmark scores the
degradation as an improvement. A consistency measure that prefers the input
to its restoration cannot evaluate restoration. Second, \textbf{both
dimensions order UAV above MGLD-VSR} --- the inverse of human judgement and
of LR-VCC's ranking --- because feature-space stability mechanically rewards
the smoother method. LR-VCC's design premises (multi-scale measurement
against real motion, anchored drift rather than pairwise stability,
reliability gating) are precisely the deltas that separate it from this
closest neighbour.

\paragraph{Severity response of the SOTA dimensions.} The ranking inversion
above could in principle be excused if the dimensions at least \emph{detected}
long-range corruption. They do not. Both dimensions were run over the
background-drift and colour-drift artefact families --- fifty full-length
clips regenerated bit-reproducibly --- under the identical
$\Delta(\text{sev } 0.02 \to 0.40)$ verdict protocol as
Table~\ref{tab:exp:verdict}. The result is universal insensitivity
(Table~\ref{tab:exp:sotaseverity}): \textbf{0 of 20 (dimension, family, base)
cells respond} --- every cell is \textsc{flat}, including
background-consistency on background drift, the dimension's namesake failure
mode --- and 14 of the 20 sub-threshold drifts run in the \emph{rewarding}
direction. LR-VCC is clean on 7 of the same 10 (family, base) conditions.

\begin{table}[h]
\centering
\caption{Severity response of the SOTA consistency dimensions on the
long-range artefact families (five bases each), versus LR-VCC on the same
clips. Full per-base tables: \texttt{vbench\_sota\_verdicts.md}.}
\label{tab:exp:sotaseverity}
\small
\begin{tabular}{llccc}
\hline
\textbf{Dimension} & \textbf{Artefact} & \textbf{Clean} & \textbf{$\Delta$ range} & \textbf{LR-VCC clean} \\
\hline
background\_consistency & background\_drift & 0/5 & $[-0.012, +0.004]$ & 2/5 \\
background\_consistency & colour\_drift & 0/5 & $[+0.000, +0.006]$ & 5/5 \\
subject\_consistency & background\_drift & 0/5 & $[-0.010, +0.010]$ & 2/5 \\
subject\_consistency & colour\_drift & 0/5 & $[+0.000, +0.005]$ & 5/5 \\
\hline
\end{tabular}
\end{table}

The mechanism is designed in, not incidental. The long-video protocol these
dimensions ship with weights the cross-clip component to zero
($w_{\mathrm{clip2clip}}{=}0$): the reported score is within-2-second-clip
feature consistency, re-aggregated. A corruption that develops \emph{across}
clips --- colour ramping over minutes, a background walking toward a
replacement --- changes each 2-second window almost nothing and is therefore
invisible \emph{by construction}. This is the empirical form of the
structural argument in Chapter~\ref{ch:problem}: clip-level re-aggregation
removes exactly the signal that long-video consistency assessment requires.
Combined with the ranking result above --- degraded input scored above its
restorations --- the two SOTA dimensions fail both requirements of a
long-video consistency measure on this benchmark: they do not rank
perceptually, and they do not detect.

\section{Ablations and Sensitivity}
\label{sec:exp:ablations}

Because every sub-metric caches its per-video measurements, the composite can
be recomposed under alternative hyperparameters or with sub-metrics removed in
seconds, without re-scanning any video. Two studies exploit this.

\subsection{Hyperparameter Sensitivity}
\label{sec:exp:sensitivity}

A 52-configuration grid varies the anchored-drift decays $\beta_{D'} \in
\{0.25, 0.5, 1, 2\}$ and $\beta_{D''} \in \{1, 2, 3, 5\}$ crossed with the
softmax temperature $\tau \in \{0.1, 0.2, 0.5\}$, plus one-at-a-time
variations of $\alpha$ and $\beta_E$. Across the grid: the per-video
MGLD $>$ UAV ordering --- the metric's core perceptual-agreement claim ---
holds on every video in 50 of 52 configurations; the three-method mean order
is unchanged in 45 of 52; and the verdict matrix's clean count stays within
$[26, 32]$ of the production 29/60, with the cells that change class
concentrated in already-borderline \textsc{weak}/\textsc{flat} boundaries.
All seven order changes occur at $\tau = 0.5$: softening the softmax dilutes
the reliability weighting that the design depends on
(Section~\ref{sec:method:reliability}), collapsing the composition toward an
ungated mean. Sharp reliability weighting is thus a load-bearing choice, not
a free parameter --- and within any reasonable neighbourhood of the
production temperature, the headline results are stable.

\subsection{Leave-One-Out Sub-Metric Ablation}
\label{sec:exp:loo}

Dropping each of the seven sub-metrics in turn and recomposing tests
hypothesis H1 directly: does the composite catch what no constituent catches,
and does each sub-metric earn its place?

\begin{itemize}
    \item \textbf{Each colour-family sub-metric fails in its designed
          direction when removed.} Dropping D$'$ flips background-drift cells
          to \textsc{inverted} (the convergence pathology returns); dropping
          E degrades the colour-drift row; dropping D$''$ or D moves
          $10$--$13$ cells each, concentrated in the drift and boundary
          families they respectively target. No single sub-metric's removal
          leaves the matrix intact --- the composition is load-bearing, not
          redundant (H1 supported).
    \item \textbf{Dropping identity flips the FlashVSR-vs-MGLD comparison.}
          FlashVSR's proximity to the top of the real-model table rests on
          the identity sub-metric; with I removed, MGLD leads by a wide
          margin. Combined with the closeup-gating analysis above, the honest
          summary is that MGLD-vs-FlashVSR is an identity-weighted
          comparison, while MGLD $>$ UAV holds under every ablation.
    \item \textbf{Dropping temporal changes nothing} --- neither the
          real-model ordering nor a single verdict cell. On these surfaces T
          rarely dominates the softmax, consistent with its near-tied scores
          across well-behaved methods
          (Table~\ref{tab:exp:threemethod}); its value is diagnostic (the
          $k$-sweep analysis of Chapter~\ref{ch:problem}) rather than
          discriminative within the composite.
\end{itemize}

\subsection{Composition Provenance Audit}
\label{sec:exp:provenance}

Recomposing from cached measurements also enabled a full reproduction audit
of every published composite. The audit reproduced the production outputs
bit-exactly and surfaced three composition-era inconsistencies in interim
tables (a reliability-formula change mid-campaign, a close-up gate applied to
only one method, and a stale identity input for one method), all corrected in
the canonical protocol used throughout this chapter. The uniform recompose
shifts the clean count from 28/60 to 29/60 and changes six cell classes ---
none affecting any headline claim. The episode reinforces a reproducibility
practice this thesis adopts throughout: cache raw measurements, re-derive
scores, and gate every published table on bit-exact reproduction.

\section{Limitations}
\label{sec:exp:limitations}

Every residual failure in this chapter has a named mechanism, recorded here
both as honest scope statements and as future-work targets.

\textbf{Cartoon / scene-cut-heavy content.} The BrRLK base carries most of
the matrix's inversions. Its frequent hard scene cuts mean the opening-window
anchor of D$'$ and D$''$ does not represent the video: natural
anchor-distance variation dominates injected corruption. An adaptive,
scene-cut-aware anchor is the natural fix
(Chapter~\ref{ch:conclusion}).

\textbf{Pure horizontal mirroring is invisible.} flip\_horizontal preserves
colour marginals by construction and CLIP embeddings are documented to be
robust to horizontal flips, so all seven sub-metrics are blind to it --- a
designed-in null result documented as a metric limit. A mirror-sensitive
sub-metric (pixel-correlation or pose-based) would close it.

\textbf{Identity families remain the weakest rows.} The slow-fast pooling
pathology (Section~\ref{sec:problem:battery}, Finding~4) is only partially
mitigated: the dispersion gate designed for it could not be calibrated
defensibly on two single-face bases (separation $0.003$) and ships parked.
Progressive identity drift in particular responds on only two of five bases.

\textbf{Scale of the benchmark.} Five base videos and three (soon five) SR
methods support the mechanism-level claims made here, but community-benchmark
claims would need an order of magnitude more content and human-judgement
calibration --- deliberately deferred (Chapter~\ref{ch:conclusion}).
