% Chapter 6: RoPE Extrapolation Study
\chapter{RoPE Extrapolation in Streaming Diffusion Video Super-Resolution}
\label{ch:rope}

% =========================================================================
% DRAFT SKELETON (Jul 11). Canonical sources for the full write-up:
%   docs/notes/2026-07-11-rope-extrapolation-findings.md   (capstone verdicts)
%   docs/notes/2026-07-02-flashvsr-rope-site.md            (architecture + gates)
%   reports/rope_timespace_sensitivity_matrix.md            (standalone study incl. PI theory)
%   reports/position_interpolation_presentation.md          (PI figures + argument)
%   docs/notes/2026-07-16-rope-chapter-notes.md             (corrected canonical numbers)
%   reports/figures/rope_probe_udm10_gt.md                 (vs-GT verdicts)
%   reports/figures/dpp_causal_verdict.md                  (D'' causal check)
%   results/rope_probe/*                                   (raw JSONs)
% =========================================================================

\section{Motivation and Research Questions}
\label{sec:rope:motivation}

The real-model evaluation of Chapter~\ref{ch:experiments} left a pointed question open:
FlashVSR --- within $0.012$ of the top composite score and the outright winner on
identity preservation --- exhibits the worst long-range semantic drift (sub-metric
D$''$) of the three methods --- and FlashVSR is precisely the method whose streaming
design lets its temporal RoPE index grow without bound as the video lengthens. Its stock frequency table caps a single pass at 4{,}089 frames
($\approx 2.3$ minutes), and its trained temporal window covers only $\sim$81 frames.
Position-encoding extrapolation is therefore the natural suspect for long-video
degradation, and the same suspicion extends to the spatial axes at high resolution.

The study decomposes extrapolation into two hypotheses:

\begin{description}
    \item[H0 (Magnitude).] Absolute position offsets beyond the trained window degrade
        quality.
    \item[H1 (Geometry).] Distortion of relative-distance geometry (dilation or
        compression of position spacing) degrades quality.
\end{description}

\section{The Position-Injection Instrument}
\label{sec:rope:instrument}

A runtime position-injection hook swaps any of the three RoPE frequency tables (t/h/w)
for a wrapper that transforms baseline positions --- shift ($p \mapsto p + k$), stretch
($p \mapsto s \cdot p$), continuous-position interpolation (non-integer positions,
resolving the integer-rounding confound), and modulo bounding --- with on-demand table
extension past the stock 1{,}024 rows. The instrument satisfies three faithfulness
gates: (i) \emph{bit-exact when idle} --- with the hook installed but no transform
active, output drift is $0.0$ against a measured $0.0$ nondeterminism floor; (ii)
\emph{engagement-checked} --- every run asserts the transform actually fired; (iii)
\emph{repository-pristine} --- zero modification of the tagged FlashVSR codebase, with
48 unit tests covering the hook.
% TODO(Ch6 pass): hook mechanism detail (which module attribute is swapped), axis
% parameter (t/h/w), extended-table construction.

\section{Experimental Design}
\label{sec:rope:design}

% TODO(Ch6 pass): describe the arms —
% (1) temporal shift sweep k in {2..996} on hhsz (85 frames), self-consistency scoring;
% (2) stretch sweep s in {2..64} (self) and fine/continuous sweeps;
% (3) vs-GT verdicts: DOVE-UDM10, 10 clips x 22 conditions, pyiqa in the DOVE RGB
%     convention; baseline FlashVSR 24.02 dB (MGLD verified at 24.23 on same protocol);
% (4) D'' causal check on the 2412–5000-frame real-SR videos: segmented vs single-pass
%     vs magnitude-bounded (mod 336) arms;
% (5) spatial axis (h/w) label sweeps on UDM10;
% (6) real resolution-extrapolation ladder on YouHQ40: latent grids 48^2 -> 72^2 -> 96^2
%     (= 1x/1.5x/2x the trained extent; padding quantises grids to multiples of 8),
%     stock vs spatial-PI;
% (7) collapse-decomposition arms (Jul 16): content-correct scoring, pinned topk,
%     blur-isolation, intermediate grids 80/88 — see corrected Results below.
% Emphasise the method point: self-consistency scoring alone is insufficient (see
% Results) — real GT references are required.

\section{Results}
\label{sec:rope:results}

\subsection{Absolute-Position Offsets Are Free}
% TODO(Ch6 pass): shift +996 (50x beyond trained window) costs +0.001 dB vs GT — H0
% rejected on every axis (temporal to position 996; spatial offsets 8/24), in every
% interaction tested. The stock 4089-frame ceiling is an implementation artefact of the
% fixed table: the extended table sustains a 5009-frame single pass (11.6 GiB VRAM).

\subsection{Relative-Distance Geometry Is What Bites}
% TODO(Ch6 pass): dilation costs monotonically: -0.12 dB @ s=1.25, -0.22 @ 1.5,
% -0.25 @ 2.0, -0.95 @ 3.0; compression s=0.5 costs -0.66 dB; mild compression s=0.75
% (the PI zone) is free (+0.01 dB). Under streaming, relative distances stay in-range
% until s ~ 2.6, so small s<=2 costs are within-window geometry effects; s=3 is the
% first genuinely out-of-window point. Continuous-PI builder validated (identity
% 53.39 dB); continuous compression genuinely cheaper than integer-rounded by
% +0.5–1.5 dB.

\subsection{Self-Consistency Does Not Predict Quality}
% TODO(Ch6 pass): s=0.75 and s=1.5 share identical self-PSNR (31.8) but have opposite
% GT verdicts (+0.01 vs -0.22 dB). Methodological finding for the field:
% position-perturbation studies need real references.

\subsection{Long-Video Drift Is Not Positional}
% TODO(Ch6 pass): D'' causal check — segmented vs single-pass vs magnitude-bounded arms
% indistinguishable (<1% relative spread) on 2412–5000-frame videos. FlashVSR's worst
% benchmark cell belongs to the streaming KV-cache/generation mechanism, not position
% encoding. This also certifies the Chapter 5 benchmark row was fair (segmentation did
% not disadvantage FlashVSR).

\subsection{The Time--Space Sensitivity Matrix}
% TODO(Ch6 pass): port matrix from rope_sensitivity_matrix.md — translation invariance
% universal across all three RoPE axes; a PI-free zone (mild compression) exists on
% every axis; space is ~2.5x more sensitive than time at extreme geometry distortion.

\subsection{Real Resolution Extrapolation}
% TODO(Ch6 pass): YouHQ40 ladder, grids 48/72/80/88/96 (1x..2x trained extent):
% quality is FLAT across the whole ladder (24.55–24.78 dB) — real resolution
% extrapolation is quality-free to at least 2x the trained spatial extent with stock
% positions. Spatial-PI at 1.5x *hurts* (-0.56 dB; factor 0.67 is past the free zone):
% let spatial positions extrapolate, do not compress them.
% METHODS NOTE (write honestly): an early scoring pass reported an -11 dB "collapse"
% at the top rung; decomposition arms (pinned sparsity delta -0.03, blur arm flat,
% intermediate grids flat) traced it to a scoring-geometry artefact — the padded
% output frame was resized against GT at mismatched magnification. Second instance in
% this work where a scoring-path subtlety flipped a conclusion (cf. self-consistency
% vs quality); both caught because raw frames + re-scorable JSONs were retained.

\subsection{Why Position Interpolation Works: Interpolation vs.\ Extrapolation}
% TODO(Ch6 pass): port from reports/rope_timespace_sensitivity_matrix.md §5 and the
% PI presentation. Core argument: attention consumes relative phase (p-q)·theta;
% training covers ~20 discrete distances per frequency; PI places new inputs BETWEEN
% trained points (smooth learned function -> well-behaved), extrapolation places them
% BEYOND (unconstrained). Killer figure: rotation-change magnitude depends only on
% |s-1|·d (perfectly symmetric in compression/dilation) yet measured quality is
% asymmetric -> WHERE phases land matters, not how far they moved. Boundary of
% zero-shot PI: s=0.5 puts neighbour distances below the minimum trained distance
% (1.0) — the self-vs-neighbour transition. Figures: reports/figures/pi/fig1–fig4
% (regenerate as PDF/PGF for LaTeX; script in chapter-notes doc).

\section{Implications}
\label{sec:rope:implications}

% TODO(Ch6 pass):
% - Position encoding is exonerated: improvement effort for long-video quality should
%   target the streaming KV-cache/generation mechanism and resolution-scaling mechanics
%   (sparsity/windowing), not RoPE.
% - Window-extension guidance: temporal extension to ~1.33x (21 -> 28 latents) with
%   continuous-PI positions is predicted RoPE-loss-free; beyond that, geometry costs
%   grow. Instrument + predictions handed to the window-extension effort.
% - PI helps only in narrow regimes and can hurt (spatial <=1.5x); blanket application
%   of LLM context-extension recipes to video DiTs is not supported.

\section{Limitations}
\label{sec:rope:limitations}

% TODO(Ch6 pass): all vs-GT evidence is temporally within-window (29–31-frame GT
% clips); deep-length (>81-frame) quality-vs-position curves lack GT (long-HR source
% acquisition pending). Single model (FlashVSR 1.3B distilled); spatial trained-extent
% taken from the distillation resolution (48 latents).
